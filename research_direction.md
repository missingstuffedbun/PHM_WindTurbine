# 基于稀疏传感与物理约束图学习的风机结构健康监测

## 1. 项目简介

本项目研究风力发电机在**稀疏传感条件下的结构状态恢复问题**：利用有限监测信息，恢复风机关键结构响应。

研究基于公开的 **Björkö Wind Turbine Structural Monitoring Dataset**，构造不同程度的传感器稀疏与失效场景。

核心问题：

> **在监测信息不完整时，能否利用风机部件间的结构关联与物理规律，准确恢复不可观测的结构状态？**

对比方法：

1. 数据驱动模型（如 Transformer、GNN 等）；
2. 物理约束模型（PINN）。

重点分析不同稀疏程度和失效模式下，PINN 是否能提升恢复稳定性。

---

## 2. 研究问题

### RQ1：稀疏监测下，风机关键结构状态能否被有效恢复？

通过移除部分传感器模拟稀疏场景，以完整数据作为真实状态验证。

### RQ2：结构拓扑信息是否能提升状态恢复能力？

比较不考虑结构关系的模型与利用风机结构图的 GNN。

### RQ3：引入物理约束后，模型在严重稀疏和传感器失效下是否更稳定？

比较数据驱动模型与 PINN，分析物理知识对鲁棒性的影响。

---

## 3. 数据说明

### 3.1 数据集

**Björkö Wind Turbine Structural Monitoring Dataset**

来自真实风机长期运行监测，共约 69 个测量信号。

### 3.2 主要信号

| 类别 | 信号 | 说明 |
|------|------|------|
| 结构响应 | TMBNS / TMBEW / TMBTOR | 塔底弯矩与扭矩（主要恢复目标） |
| 结构响应 | NAX / NAY / NAZ | 机舱加速度 |
| 运行状态 | PAB1-3 / B1POS-3 / XTurbSpeed / YP | 桨距角、叶片位置、转速、偏航 |
| 环境条件 | WS30 / WD30 / WSN / WDN / AIRTN / AIRHNA | 风速、风向、温度、湿度 |

### 3.3 信号选择原则

- 优先使用 metadata 中标记为可靠的信号；
- 不可靠或未校准的信号不作为 ground truth；
- 保持输入与输出在物理意义上的独立性，避免数据泄漏。

---

## 4. 实验设计

### 4.1 基本流程

```text
完整风机监测数据
        │
        ▼
数据清洗与变量选择
        │
        ▼
构造完整监测状态
        │
        ├───────────────┐
        ▼               ▼
稀疏监测场景         完整状态
        │               │
        ▼               │
   AI模型预测           │
        │               │
        ▼               │
  恢复结构状态 ─────────┘
        │
        ▼
性能评价
```

---

## 5. 对比方法

### 5.1 数据驱动模型

不引入物理约束，仅利用数据中的时间与空间关系学习映射。

可选模型：

- **LSTM / GRU**：学习时间依赖；
- **Transformer**：学习长程时序关系；
- **GNN / GNN+时序模型**：利用风机结构拓扑学习空间关联。

### 5.2 物理约束模型（PINN）

在数据驱动模型基础上，引入物理约束作为正则项。

物理约束可包括：

- 塔底弯矩与推力、倾覆力矩之间的平衡关系；
- 结构响应的连续性与平滑性；
- 运行状态—结构响应之间的合理关系。

目标：在传感器稀疏或失效时，利用物理知识稳定预测。

---

## 6. 稀疏监测场景

### S0：完整监测

所有选定传感器可用，作为性能上限。

### S1：中等稀疏

减少部分结构监测信息，保留主要运行状态和环境变量。

### S2：严重稀疏

进一步减少结构监测信息，测试模型在有限信息下的恢复能力。

### S3：关键传感器失效

模拟特定区域传感器故障，例如：

- Tower sensor failure；
- Nacelle sensor failure；
- Rotor sensor failure。

---

## 7. 主要预测任务

**Structural State Reconstruction**

恢复目标：

- TMBNS
- TMBEW
- TMBTOR

```text
Sparse monitoring information
              │
              ▼
       AI reconstruction
              │
              ▼
Tower structural response
```

完整监测数据中的真实 TMBNS、TMBEW、TMBTOR 作为 ground truth。

---

## 8. 核心假设

> 风机结构拓扑信息与物理知识能够弥补稀疏监测条件下的信息不足，使 PINN 在传感器缺失场景下比纯数据驱动模型更稳定。
