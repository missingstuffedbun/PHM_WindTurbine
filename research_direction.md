# 基于稀疏传感与物理约束的风机塔架结构健康监测

## 1. 项目简介

本项目研究风力发电机在**稀疏传感条件下的结构状态恢复问题**：利用有限监测信息，恢复风机关键结构响应。

研究基于公开的 **Björkö Wind Turbine Structural Monitoring Dataset**，构造不同程度的传感器稀疏与失效场景。

核心问题：

> **在监测信息不完整时，能否利用风机部件间的结构关联与物理规律，准确恢复不可观测的结构状态？**

对比方法：

1. 数据驱动模型（如 LSTM、GRU、TCN、Transformer 等）；
2. 物理约束模型（PINN）。

重点分析不同稀疏程度和失效模式下，PINN 是否能提升恢复稳定性。

---

## 2. 研究问题

### RQ1：稀疏监测下，风机关键结构状态能否被有效恢复？

通过移除部分传感器模拟稀疏场景，以完整数据作为真实状态验证。

稀疏的组织方式由场景配置中的 `level` 控制（定义见 `config/scenarios.yaml`）：
- `channel`：按通道随机屏蔽（部分测点未安装 / 长期失效）；
- `timestep`：按时间点随机屏蔽（采样丢包 / 间歇采集，所有通道在缺失时刻同时不可用）；
- `segment`：按时间段整段置空（通信中断 / 停机导致的连续缺失）。

三者代表不同的信息缺失机理：同样的 `observable_ratio` 下，`segment` 的连续缺失对时序
依赖与相关性类物理约束的破坏强于 `timestep` 的随机缺失，而 `channel` 缺失的是整个测点
的静态信息。因此 RQ1 / RQ2 的结论（以及物理约束的增益边界）需按 `level` 分别讨论。

### RQ2：引入物理约束后，模型在严重稀疏和传感器失效下是否更稳定？

比较纯数据驱动模型与 PINN，分析物理知识对鲁棒性的影响。

### RQ3：不同数据驱动 backbone 对稀疏监测的适应能力有何差异？

比较数据驱动模型与 PINN，分析物理知识对鲁棒性的影响。

---

## 3. 数据说明

### 3.1 数据集

**Björkö Wind Turbine Structural Monitoring Dataset**

来自真实风机长期运行监测，共约 69 个测量信号。

### 3.2 主要信号

| 类别 | 信号 | 说明 |
|------|------|------|
| 结构响应 | TMBNS / TMBEW / TMBTOR | 塔底弯矩与扭矩（主要恢复目标） |
| 结构响应 | NAX / NAY / NAZ | 机舱加速度 |
| 运行状态 | PAB1-3 / B1POS-3 / XTurbSpeed / YP | 桨距角、叶片位置、转速、偏航 |
| 环境条件 | WS30 / WD30 / WSN / WDN / AIRTN / AIRHNA | 风速、风向、温度、湿度 |

> 注意：TMBNS / TMBEW 是塔基弯矩矢量在**地理固定坐标系**下的两个正交分量，
> 不是两个独立的结构响应量，也不随机舱偏航转动；二者必须作为二维矢量处理
> （见 `physics.md` §8）。TMBTOR 是绕竖轴的扭转分量，与它们解耦。

### 3.3 信号选择原则

- 优先使用 metadata 中标记为可靠的信号；
- 不可靠或未校准的信号不作为 ground truth；
- 保持输入与输出在物理意义上的独立性，避免数据泄漏。

---

## 4. 实验设计

### 4.1 基本流程

```text
完整风机监测数据
        │
        ▼
数据清洗与变量选择
        │
        ▼
构造完整监测状态
        │
        ├───────────────┐
        ▼               ▼
稀疏监测场景         完整状态
        │               │
        ▼               │
   AI模型预测           │
        │               │
        ▼               │
  恢复结构状态 ─────────┘
        │
        ▼
性能评价
```

---

## 5. 对比方法

### 5.1 数据驱动模型

不引入物理约束，仅利用数据中的时间与空间关系学习映射。

可选模型：

- **LSTM / GRU**：学习长短期时序依赖；
- **TCN**：因果空洞卷积，提取局部到全局时序特征；
- **Transformer**：学习长程时序关系。

### 5.2 物理约束模型（PINN）

在数据驱动模型基础上，引入基于结构动力学的**物理约束**。

> 说明：此处是**物理约束**，不是数值正则。物理约束把已知的物理规律以软残差形式
> 注入训练；对参数范数的惩罚才是正则项（由 `weight_decay` 承担）。
> 早期版本曾有一条“时序平滑”项，因实现与语义均不成立已移除（见 §5.2.3 第 4 点）。

#### 5.2.1 塔底弯矩与推力 / 倾覆力矩的平衡式

塔底截面（南北 NS / 东西 EW）的倾覆力矩由塔顶传递的载荷产生，准静态主导平衡式为

```text
M_base ≈ T · h,   T = ½ · ρ · A · C_T(λ, β) · V²,   A = πR²,   λ = ωR / V
```

风速通道不可用时由传动链反推：`T = Q·ω·C_T / (V·C_P)`。

该式的**简化假设**（A1–A7）、**量纲一致性推导**、**z-score 标准化下的形式不变性**、
**误差范围**（稳态 ±20%（1σ），阵风 / 变桨 / 启停瞬态 30%–40%）与**适用边界**
详见 **[`physics.md`](physics.md)**，此处不再展开。

#### 5.2.2 坐标系：为什么约束施加在 fore-aft / side-side 而不是 NS / EW

`TMBNS / TMBEW` 的测量坐标系是**固连于塔架的地理坐标系**，而式 (1) 的气动推力作用在
**随机舱偏航的机舱坐标系**上，两者的夹角由偏航角 `YP` 决定且随时间变化。因此：

- NS / EW 都不是“顺风向”或“侧向”的固定对应量，对其中任一通道单独施加推力类先验
  都是**错误先验**；
- 逐通道的 R² 会把“推力预测得好不好”与“当时风从哪个方向吹”混在一起，是**不稳定指标**，
  换一个风向窗口（如 20 Hz 版本）结论可能反转。

实现用 `YP`（已在输入特征中）把这个矢量旋转到机舱坐标系后再按机制分开处理：

```text
ψ = YP mod 360
M_fa =  M_NS·cos ψ + M_EW·sin ψ     # 顺风向（fore-aft）：推力主导
M_ss = −M_NS·sin ψ + M_EW·cos ψ     # 侧向（side-side）：湍流 / 摆振主导
```

旋转必须在**去标准化后的原始域**进行（σ_TMBNS / σ_TMBEW ≈ 0.64，在标准化域合成幅值
与物理域 \|M\| 的相关系数只有 0.854）。实测依据见 `physics.md` §8。

#### 5.2.3 物理约束来源与实现对应

物理约束来自以下可量化的物理关系（括号内为其在 `models/physics.py` 中的实现）：

1. **M_fa ↔ 气动推力代理**（式 (1)(2)：M_base ≈ T·h）。推力 T 不可直接测量，实现按
   `RST2`（轴扭矩 Q ∝ V² ∝ T）→ `ω²`（由 λ = ωR/V 恒定）→ `V²`（WSN / WS30 / WindEst）
   依次回退选取代理；**只施加在顺风向分量上**。
2. **侧向分量 M_ss 不施加推力类约束**。实测 R² 仅 0.22、与 RST2 相关系数 +0.31，
   它由横风湍流与塔架摆振主导，套推力先验只会引入错误先验。
3. **TMBTOR ↔ ω²**：气动扭矩 Q ∝ ω²，与 `physics.md` 式 (3) 同源。
4. ~~机舱惯性 \|M\| ↔ 加速度~~ 与 ~~时序平滑性~~：**已移除**。前者所用的机舱加速度
   驱动实测弱于推力代理，且 \|M\| 混入了与推力本无关的侧向分量；后者属数值正则而非
   物理约束，且 `DataLoader(shuffle=True)` 使 batch 内相邻样本在时间上并不相邻，
   该实现实际惩罚的是批内输出方差，语义不成立（详见 §5.2 开头说明）。

各条约束均以 **1 − ρ²** 的软残差施加（取值 [0,1]、量纲无关、对仿射变换不变，等价于
逐 batch 拟合一次未知标定）；驱动通道被 mask 时该约束自动跳过，因此物理约束是
**场景感知**的 —— 这正是 §6 各场景语义差异的来源。

目标：在传感器稀疏或失效时，利用物理知识稳定预测，并明确物理约束有效的边界条件。

---

## 6. 稀疏与失效场景定义

### S0：完整监测

所有选定传感器可用，作为性能上限。

### S1：中等稀疏

减少部分结构监测信息，保留主要运行状态和环境变量。

### S2：严重稀疏

进一步减少结构监测信息，测试模型在有限信息下的恢复能力。

### S3：关键传感器失效

模拟特定区域传感器故障：

- **Meteo sensor failure**（`s3_meteo_failure`）：WS30 / WD30 / WSN / WDN 风速风向不可用。
  属**数据侧失效**：物理约束的主驱动（轴扭矩 RST2、转速 ω²）完好，只有最末一级的 V²
  兜底代理被切断，物理约束基本不受影响；
- Nacelle **accelerometer** failure（`s4_nacelle_accel_failure`）：仅机舱三轴加速度失效
  （YP 偏航位置保留）。注意现行物理约束**已不再使用机舱加速度**（见 §5.2.2），
  因此该场景现在是**纯数据侧失效**，不再构成“物理驱动失效”对照；
- Shaft sensor failure（`s5_shaft_failure`）：转速（XTurbSpeed1 / TurbSpeed2）与轴扭矩
  RST2 失效。这三路正是 fore-aft 约束与扭矩约束的全部核心驱动，失效后会自动回退到
  最后一级 V² 代理，故本场景是当前**唯一**真正让物理约束近乎退场的场景。
  转子侧的桨距角 PAB1-3 与叶片位置 B1POS-3 属气动状态信息，不并入本场景
  （屏蔽它们会与 S1 / S2 的随机稀疏语义重叠）。

> 重要：物理约束的驱动集合已在本次重构中改变（不再依赖机舱加速度），S3 / S4 / S5 的
> 因果语义随之改变——原先“S3 数据侧 / S4、S5 物理驱动侧”的对照不再成立，现为
> “S3、S4 均属数据侧，仅 S5 属物理驱动侧”。若要保留三方对照，建议新增一个仅屏蔽
> **RST2** 的场景（主驱动之一失效、转速 ω² 仍在），或一个“气象 + 传动链同时失效”的
> 宽口径场景。

> 原定的 “Tower sensor failure”（屏蔽 TMBNS / TMBEW / TMBTOR）已删除：这三个信号
> 是预测目标，本就不进入输入，屏蔽它们与 S0 在数据层面完全等价，属于空转场景。

---

## 7. 主要预测任务

**Structural State Reconstruction**

恢复目标：

- TMBNS
- TMBEW
- TMBTOR

```text
Sparse monitoring information
              │
              ▼
       AI reconstruction
              │
              ▼
Tower structural response
```

完整监测数据中的真实 TMBNS、TMBEW、TMBTOR 作为 ground truth。

---

## 8. 核心假设

> 物理知识与数据驱动模型相结合能够弥补稀疏监测条件下的信息不足，使 PINN 在传感器缺失场景下比纯数据驱动模型更稳定。

---

## 9. 已知局限与后续工作

以下问题已由诊断脚本确认但**尚未修改**，解读实验结论时需一并考虑：

1. **数据不连续**：`processed.csv` 并非单一连续时间序列，而是由**多个不相邻时间段**
   拼接而成（100 Hz 版 9 段，最大断点约 245 天，可用 `check_frame.py` 复现）。影响有两点：
   - 滑窗跨段会生成物理上不存在的样本；
   - train / val / test 按行顺序切分会把不同时间段分入不同集合，指标可比性下降；
   - 跨段统计还会把不同工况的方差叠加，使各分量的 σ 相对均值偏大。

2. **评价仍是逐通道 R²**：`utils/metrics.py` 当前对 TMBNS / TMBEW / TMBTOR 分别给出
   RMSE / MAE / MAPE / SMAPE / R²。按 §5.2.2，NS / EW 的评价应改为**矢量形式**
   ——在物理域反标准化后给幅值误差 Δ\|M\| 与方向误差 Δφ，否则指标会随风向窗口漂移。

3. **场景定义与物理驱动待重新同步**：`config/scenarios.yaml` 的注释与
   `utils/analysis.py: CONSTRAINT_DRIVERS` 仍引用重构前的约束编号（C1 加速度 / C3 / C4），
   与 `models/physics.py` 现行实现不一致，会影响"负迁移归因"部分的正确性。
