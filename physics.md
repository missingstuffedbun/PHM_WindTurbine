# 物理约束说明（塔底弯矩 – 推力 / 倾覆力矩平衡式）

本文给出项目物理约束的理论依据：**塔底弯矩与推力 / 倾覆力矩的平衡式**、**简化假设**、
**量纲一致性推导**与**该假设的误差范围**。实现见 `models/physics.py`。

> 定性说明：本文所述为**物理约束**，不是数值正则。物理约束把已知物理规律以软残差形式
> 注入训练；对参数范数、输出平滑性的惩罚才是正则项（由 `weight_decay` 与时序平滑项承担）。

---

## 1. 平衡式

塔底截面（南北 NS / 东西 EW 两个正交分量）的倾覆力矩由塔顶传递的载荷产生。取准静态
主导项：

```text
M_base ≈ T · h                                            (1)
T = ½ · ρ · A · C_T(λ, β) · V²,   A = πR²,   λ = ωR / V    (2)
```

风速通道不可用（被 mask 或不可靠）时，由传动链扭矩 / 功率反推推力：

```text
P = Q · ω = ½ ρ A C_P V³   ⇒   T = P·C_T / (V·C_P) = Q·ω·C_T / (V·C_P)   (3)
```

保留次要项时的完整形式：

```text
M_NS = T·h·cosφ + m_RNA·g·e_NS − m_RNA·a_NS·h + M_tower,aero + M_grav,tower   (4)
```

---

## 2. 简化假设

| 编号 | 假设 | 内容 |
|------|------|------|
| A1 | 准静态 / 刚性塔架 | 塔架视为底部固支悬臂梁，塔底弯矩由塔顶准静态载荷决定；忽略塔架一阶弯曲模态（典型 2–3 MW 机组 f₁ ≈ 0.3–0.4 Hz）的弹性恢复项 −k·x 与阻尼项 −c·ẋ，即忽略动力放大与相位滞后 |
| A2 | 推力集中作用 | 叶轮推力合力作用于轮毂中心，力臂 L = h_hub − h_base ≈ h_hub；沿叶展分布载荷只改变合力作用点微小偏移，对塔底倾覆一阶项无贡献 |
| A3 | 主导项保留 | 重力偏心项 m_RNA·g·e 为常数偏置，在标准化数据中退化为常数，被软约束回归的截距吸收；机舱惯性项 m_RNA·a 为小量，作为伴随项而非主项 |
| A4 | 气动系数缓变 | 运行区间内 C_T(λ, β) 视为常数（λ 高、β = 0 时接近 8/9 贝茨推力上限区，取 C_T ≈ 0.8）；额定风速以上变桨使 C_T 降至 0.2–0.3，变桨区需修正或降权 |
| A5 | 来流对准 | 偏航误差 \|φ\| ≲ 10°，投影以 cosφ ≈ 1 处理；忽略风剪切、塔影与偏航不对称引起的侧向分量 |
| A6 | 常数几何与物性 | ρ ≈ 1.225 kg/m³、扫风面积 A、轮毂高度 h 均为常数，并入可学习比例系数，无需显式给出 |
| A7 | 幅值合成 | 以 \|M\| = √(M_NS² + M_EW²) 作为倾覆力矩幅值，规避 A5 的投影误差在两分量上的分配问题（代价：丢失方向信息） |

---

## 3. 量纲一致性推导

采用 SI 基本量纲 M（质量）、L（长度）、T（时间）逐项核验：

```text
[ρ]   = kg·m⁻³ = M L⁻³
[A]   = m²     = L²
[C_T] = 1      = 无量纲
[V²]  = m²·s⁻² = L² T⁻²
⇒ [T] = M L⁻³ · L² · L² T⁻² = M L T⁻² = kg·m·s⁻² = N   ✓ 与力的量纲一致

[h] = m = L   ⇒ [T·h] = N·m   ✓ 与 TMBNS / TMBEW（工程单位 kN·m）量纲一致

惯性项    [m_RNA·a·h] = kg · m·s⁻² · m = N·m   ✓
重力项    [m_RNA·g·e] = kg · m·s⁻² · m = N·m   ✓
扭矩反推  [Q·ω] = N·m·rad·s⁻¹ = W；[Q·ω / V] = W/(m·s⁻¹) = N；
          再乘无量纲的 C_T/C_P 仍为 N                    ✓
```

工程单位对照：T [kN] × h [m] → M [kN·m]，与数据集塔底弯矩通道一致。

---

## 4. 标准化（z-score）下的形式不变性

数据经 StandardScaler 处理：x̂ = (x − μ)/σ。仿射变换不改变线性一致性残差的形式——
若 M = α·(T·h) + β，则 M̂ = (α σ_Th / σ_M)·(T·h)^ + b′，仍是同一线性关系。因此约束
以"可学习比例系数 + 截距"的软残差施加，等价于对物理量做未知仿射标定，绝对常数
（ρ、A、h、C_T）被自动吸收，无需显式给出。

需要注意：标准化后的平方不等于平方的标准化，

```text
V̂² = (V² − 2μ_V·V + μ_V²) / σ_V²
```

即 V̂² 等价于"V² 的标准化 + 一个线性 V 项 + 常数"。在 V ∈ [μ−2σ, μ+2σ] 内
V² = μ² + 2μσV̂ + σ²V̂²，二次项与一次项幅值之比约为 σ_V/(2μ_V)；对风速变异系数
CV = σ/μ ≈ 0.2–0.3，该比值为 10%–15%，由此引入的附加误差计入下节 E2。

---

## 5. 误差范围

| 编号 | 误差来源 | 量级 | 说明 |
|------|----------|------|------|
| E1 | 准静态假设（忽略塔架一阶模态动力放大） | 稳态 < 10%；阵风 / 启停 / 共振 10%–30% | 采样 20 Hz，塔架一阶模态 ~0.3 Hz 可被激励 |
| E2 | C_T 常数假设 + 标准化下 V̂² 的仿射近似 | 10%–20% | λ ∈ [4, 9] 时 C_T 由 ~0.9 降至 ~0.7；变桨区不适用 |
| E3 | 力臂 h、偏心距 e 的几何误差 | ±2% | 几何常数，误差线性传递 |
| E4 | 机舱惯性项 m_RNA·a 的占比 | 正常运行 3%–10%；瞬态可达 15% | 相对额定推力的占比 |
| E5 | 塔身分布气动载荷与自重二阶效应 | 3%–8% | 塔筒迎风面积约占扫风面积 5%–10% |
| E6 | 偏航误差 φ ≤ 10°（cosφ ≈ 1） | ≤ 1.5% | 1 − cos10° = 1.52% |

合成（RSS，稳态）：√(10² + 15² + 2² + 10² + 5² + 1.5²) ≈ 21%，取 **±20%（1σ）**；
极端工况（额定风速附近变桨、强阵风、启停瞬态）可扩大到 **±30%–40%**。

---

## 6. 适用边界

- 该平衡式只能作为**软约束（一阶主导关系）**使用，不能作为硬约束或等式替代监督信号；
- 以"可学习比例系数 + 自适应权重"加入损失，权重不超过数据项；
- 对应驱动通道被 mask 时自动跳过该约束；
- 适用区间：稳态运行、风速处于切入–额定之间、变桨角小（β ≈ 0）；
  额定风速以上变桨区与启停瞬态应降权或不施加。

---

## 7. 与 `models/physics.py` 实现的对应

| 实现项 | 物理来源 |
|--------|----------|
| 约束 1：\|M\| ↔ 机舱加速度幅值 | 式 (4) 的机舱惯性伴随项 m_RNA·a·h（A3 中的小量项，作为可观测代理） |
| 约束 3：TMBTOR ↔ ω² | 气动扭矩 Q ∝ ω²，与式 (3) 同源 |
| 约束 4：\|M\| ↔ ω | 由 T ∝ V²、λ = ωR/V 得到的推力代理项，是式 (1)(2) 在风速不可用时代理项 |
| 约束 5：时序平滑性 | **数值正则，非物理约束**，用于抑制非物理高频跳变 |
